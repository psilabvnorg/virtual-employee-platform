# Google OAuth client ID (from Google Cloud Console)
# Must have Authorized JavaScript origins: http://localhost:5174 (dev) + your production URL
# Must have Authorized redirect URIs if using server-side flow
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com
